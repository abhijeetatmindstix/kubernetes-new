apiVersion: v1 # String
kind: Pod  # String
metadata: # Dictionary
  name: myapp-pod
  labels: # Dictionary 
    app: myapp         
spec:
  containers: # List
    - name: myapp
      image: abhijeetatdhawale/mynginx_image2:v2
      ports:
        - containerPort: 8000